@using Microsoft.AspNet.Identity
@using Contactini.Models
@if (Request.IsAuthenticated) {
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" })) {
        @Html.AntiForgeryToken()

        string src = @ViewBag.photoLink;
        if (src == null) src = "~/Content/Images/Unknown.png";
<ul class="nav navbar-nav navbar-right">
         
    <li>
        @Html.Image(@Url.Content(src), "Profile pic","50","50")
    </li>
    }
    <li>
        @Html.ActionLink( User.Identity.GetUserName() + "!", "ChangePassword", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <li><a href="javascript:document.getElementById('logoutForm').submit()">Déconnecter</a></li>
</ul>
    }
}
else {
        <ul class="nav navbar-nav navbar-right">
            @*<li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>*@
            <li class="nav-item">
                <a class="nav-link js-scroll-trigger" id="loginLink"  href="~/Account/Login">S'identifier</a>
            </li>
            <li class="nav-item">
                <a class="nav-link js-scroll-trigger" id="registerLink" href="~/Account/Register">Nouveau Compte</a>
            </li>
        </ul>
}
